import styled from "styled-components";

const Container = styled.div`
  margin-top: 50px;
`;

const ServiceCenter = () => {
  return <Container>ServiceCenter</Container>
}
export default ServiceCenter;